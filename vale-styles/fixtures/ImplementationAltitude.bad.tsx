/**
 * Renders every rule file the checker found. The caller hands over a directory
 * and the gatherer walks it recursively, so a nested rule renders the same way
 * a top-level one does.
 */
export function RuleListBad() {
  return <ul />
}
