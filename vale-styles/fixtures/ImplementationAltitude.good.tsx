/**
 * Renders every rule file the checker found, in the order the checker reports
 * them. A nested rule is presented the same way a top-level one is.
 */
export function RuleListGood() {
  return <ul />
}
