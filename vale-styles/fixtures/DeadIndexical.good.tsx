/**
 * Renders the exit code the checker settled on, plus a badge for the second
 * return value. A caller that predates the badge may ignore it.
 */
export function ExitBadgeGood() {
  return <span />
}
