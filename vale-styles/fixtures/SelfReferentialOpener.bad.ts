/**
 * This function reports how many rows the last parse produced, and it opens
 * with the one referring expression doc-comments.md rule 2 names.
 */
export function rowsParsedOpener(): number {
  return 0
}
