/**
 * Reports every rule file the checker found. The caller hands over a directory
 * and the gatherer walks it recursively, so a nested rule is reported the same
 * way a top-level one is.
 */
export function ruleFilesFound(): string[] {
  return []
}
