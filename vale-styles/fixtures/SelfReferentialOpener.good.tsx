/**
 * Renders the row count the last parse produced, counted once at the end.
 * This component-shaped referring expression opens a SOURCE LINE rather than a
 * paragraph, and must stay silent -- the same anchor the .ts pair pins.
 */
export function RowCountGood() {
  return <span>0</span>
}
