/**
 * Renders the exit code the checker settled on. The second badge was added by
 * this slice, so a caller written before it may ignore it.
 */
export function ExitBadgeBad() {
  return <span />
}
