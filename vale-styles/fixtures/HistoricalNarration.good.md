Run the gate before handoff, because the exit code says nothing on its own.

The cache does not hold across a rebase, so run the full form there. The tokens used in this style are generic vocabulary. Do not wait any longer than one run before reporting.
