# Amendment 7 — assessment-records-die-with-the-conversation

`coach` SPEC, 2026-09-20, against tip `0e372c9`. **This amendment needs the user's signature
before `writer` runs.** It carries six items, numbered **18 to 23**, continuing the sequence.

**`spec.md` and amendments 1 to 6 are all signed and immutable.** Nothing in any of them is
reworded, renumbered or reordered. The live instruction set is eight files read in number order.

## Convergence — the ruling, before the scope ruling, because it governs it

**Ruled: this chain is converging on its original subject and diverged on its repair subject.
Both close here, and amendment 7 is the last one I recommend for this slice.**

### The count, which is the whole argument

Classify this round's six findings by what caused them.

| Cause                                | Count |
| ------------------------------------ | ----- |
| Item 17's own residues               | 5     |
| Item 14's arithmetic                 | 1     |
| New defects in the assessment record | **0** |

**That zero is the signal.** Amendments 1, 2 and 5 each found defects in the record work itself.
Amendment 6's items 12, 13 and 14 closed the last functional holes in the three skills. This round
found none. The lifecycle this slice exists to build is closed; what is still moving is the repair
of a rule the repairs introduced.

### Why item 17 alone generated five

**It retired a universal stated at an unknown number of anchors, and I treated my grep as the
census.** I found seven by searching the phrasings I had already seen. There were at least eleven.
The phrase varies — "reads the board", "no role reads it", "as prompt content", "the invoking
prompt carries" — and a grep for known wordings finds known wordings.

**That is `claim-discipline.md`'s own defect, committed against my own edit.** A count standing
without its enumeration fails silently. I wrote "seven anchors in five files" as though it were a
complete list, and it was a sample.

### What closes it, and why the set is finite this time

Two things this amendment does that no earlier one did.

1. **A census by method rather than by phrase, recorded so it can be re-run.** Item 19 carries the
   command: a union of five patterns over `.claude/**` and CLAUDE.md, every hit read rather than
   filtered. It found `.claude/references/role-file-shape.md`, which no reviewer's list carried
   and which I would have missed a second time. **A recorded method is checkable; a recalled grep
   is not.**
2. **Home reduction.** Item 21 replaces `orchestration.md`'s restatement with a pointer, taking
   the rule from five homes to four. Those four are the minimum the house allows: the canonical
   statement in `pipelines.md`, the auto-loaded routing index in CLAUDE.md, and one prohibition
   in each of the two role files that actually open board files. **A rule with four homes and a
   recorded census ends this class. A rule with an unknown number of homes did not.**

### The honest part, and the stopping condition

**If the census still missed one, a later reader finds a stale claim and fixes it as they would
any other.** That is not a reason to hold a slice open. A corpus is never proved consistent; it is
kept consistent by the checks that run on it and by readers who report what they find.

**So the stopping condition is stated rather than felt.** After this amendment, a finding in this
slice's surface goes to `backlog/done/<slice>/` as a named defect for the retrospective — the lane
whose stated job is to extract what finished work can teach. That respects the ruling against new
board items and still closes the slice.

**Two things alone should reopen it: a live contradiction between two landed instructions, or a
red gate.** Register findings, duplication, and arguable register calls go to the retrospective.

## The scope ruling

Same test as amendment 6: **does leaving it cause a wrong action that nothing later catches?**

| Finding                                     | Wrong action if left                                                                 | Ruling                        |
| ------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------- |
| 1 — `pipelines.md` read/write contradiction | `coach` refuses to write its own deliverable                                         | Land — at the source          |
| 2 — two blanket residues                    | A role reads the auto-loaded universal and refuses a named path                      | Land                          |
| 3 — `writer.md:11`                          | A contradiction three lines from the bullet item 17h corrected                       | Land                          |
| 4 — the asymmetry                           | `writer` derives a path; it is the role that opens board files                       | Land                          |
| 4 — step 5's cell                           | The seat pastes at REVIEW, or hands over no amendments                               | Land                          |
| 4 — the contracts bullet's gap              | None; it fails safe                                                                  | Marginal — rides item 18      |
| 5 — `orchestration.md` restatement          | None; it is duplication                                                              | Land — it is the home cut     |
| 5 — the vocabulary collision                | A judge reads "never rules" and declines to rule a disposition                       | Land                          |
| 6 — `/idea-promote` step 6                  | The command errors on a proposal that needs neither step                             | Land                          |
| `editor`'s two item 16a gaps                | A reader reports a pre-existing finding as self-inflicted, or reds `agent-doc-check` | Land                          |
| `editor`'s packed-bullet observation        | None directly                                                                        | Land — the additions force it |
| `prose.meta.md`'s stranded referent         | None                                                                                 | Marginal — rides item 23      |

**Nothing fails the test outright this round, and that differs from amendment 6 for a reason.**
Amendment 6 carried three genuinely marginal findings and one caught by a later read. Here, all
but two are **contradictions between two landed instructions** about who may read what. A corpus
that disagrees with itself on a read boundary is the one class that cannot wait, because the
reader cannot tell which half is current.

**Finding 5's restatement lands on a different ground from the others.** It causes no wrong
action. It lands because cutting a home is the structural half of the convergence ruling above,
and deferring it would leave the divergence mechanism in place while claiming it closed.

## What this supersedes

| Item | Supersedes                               | Files                                                              |
| ---- | ---------------------------------------- | ------------------------------------------------------------------ |
| 18   | Amendment 6's items 17a and 17b, in part | `pipelines.md`                                                     |
| 19   | nothing — three anchors no item names    | CLAUDE.md, `pipelines.md`, `role-file-shape.md`                    |
| 20   | Amendment 6's item 17h, in part          | `.claude/agents/writer.md`                                         |
| 21   | Amendment 6's item 17f, in whole         | `.claude/agents/articles/orchestration.md`, `idea-assess/SKILL.md` |
| 22   | Amendment 6's item 14, in part           | `.claude/skills/idea-promote/SKILL.md`                             |
| 23   | Amendment 6's item 16, in part           | `.claude/agents/articles/prose.md`, `prose.meta.md`                |

**Item 21 supersedes item 17f in whole**, and it is the only wholesale reversal in the chain. Item
17f put the carve-out into `orchestration.md`; item 21 takes it back out and leaves a pointer.
**Say that plainly at signature: one item of amendment 6 is undone here, deliberately.**

**The file set widens by one, to `.claude/references/role-file-shape.md`, and the census forced
it.** Every other file named above entered the set at the spec or at amendments 2 to 6.

## Item 18 — `pipelines.md` states the read/write split at the source

**Supersedes amendment 6's items 17a and 17b, in part.** The carve-out bullet's lead scopes it to
reads, so a careful reader survives. **Its trailing clauses do not carry the split, and those are
the clauses a skimmer takes.** `coach` SPEC is handed the slice name and no path, and must build
`backlog/ready/<slice>/spec.md` to write its own deliverable.

**`coach.md` is internally coherent under the split.** `pipelines.md` is the file that states the
rule canonically and fails to state its boundary, which is why the fix lands here rather than
there.

### 18a — the carve-out, plus the write grant beside it

**Find** (two whole bullets, unique in the file):

```text
- **`writer` receives the signed spec by path**, plus every amendment and the slice name. The
  prompt names `backlog/ready/<slice>/spec.md` and each `amendment-<n>.md`; no path named, no
  authority to edit.
- **A role reads a board path the prompt names, and never derives one.** That is the whole
  carve-out: its own item's `spec.md` and `amendment-*.md`, at paths it was handed. No globbing,
  no directory listing, no path built from the slice name, and nothing else under `backlog/`.
```

**Replace with:**

```text
- **`writer` receives the signed spec by path**, plus every amendment, the item numbers this
  pass runs, and the slice name. The prompt names `backlog/ready/<slice>/spec.md` and each
  `amendment-<n>.md`; no path named, no authority to edit.
- **A role reads a board path the prompt names, and never derives one.** That is the whole read
  carve-out: its own item's `spec.md` and `amendment-*.md`, at paths it was handed. Never glob,
  never list a directory, never build a read path from the slice name, and read nothing else
  under `backlog/`.
- **Writing a board file is a separate grant, and only a role's own file gives it.** `coach`
  SPEC builds `backlog/ready/<slice>/spec.md` from the slice name, because its own file names
  that deliverable. A write grant licenses no read.
```

**Every prohibition survives and each gains its scope.** "No globbing" becomes "never glob",
"no path built from the slice name" becomes "never build a **read** path from the slice name",
and "nothing else under `backlog/`" becomes "read nothing else under `backlog/`". **The
contracts bullet also gains "the item numbers this pass runs"**, which the step-2 cell already
required and this bullet omitted — a gap that failed safe, since an amendment's declared set
bounds the pass either way.

### 18b — step 5's prompt cell

`coach` REVIEW closes against the spec **and every amendment together**, which the amendment
section already says. Its cell was not one of item 17's anchors.

**Find** (one cell of the process-enabler step table):

```text
The mode by name; the signed spec; the slice name
```

**Replace with:**

```text
The mode by name; the paths of `spec.md` and every `amendment-<n>.md`; the slice name
```

**Prettier realigns the table**, as it did at item 17b. Run `npm run format` and take its output.

## Item 19 — the census, and the three anchors still stating the universal

**Supersedes nothing; no item names these three lines.**

### The method, recorded so it can be re-run

```bash
grep -rn -iE "reads? (the )?board|board.{0,20}read|prompt content|prompt carries|invoking prompt|derives? a board|never derives" .claude CLAUDE.md --include="*.md"
```

**Every hit was read rather than filtered**, and the hits that merely name an invoking prompt's
mode or a merge-protocol clause were ruled out by reading, not by a narrower pattern. **The method
is the deliverable here.** Item 17's seven anchors came from searching phrasings already seen,
which is how four survived.

**It found a third anchor no reviewer's list carried**, in a file outside the set.

### 19a — CLAUDE.md, the readiness-reference pointer

Its local subject is who reads `definition-of-ready.md`, and that is correct. It states the
correct thing as the retired universal.

**Find** (a clause; one occurrence):

```text
The audience is the orchestrating seat and the `idea-*` skills — no role reads the board.
```

**Replace with:**

```text
The audience is the orchestrating seat and the `idea-*` skills — no role assesses a candidate.
```

### 19b — `pipelines.md`, the `product` SPECIFY cell

**Find** (a parenthetical; one occurrence):

```text
(no role reads the board)
```

**Replace with:**

```text
(no role reads a proposal file)
```

**A proposal is outside the carve-out**, so the cell's point survives exactly and stops
over-claiming. `product` SPECIFY still receives the proposal's content rather than its path.

### 19c — `.claude/references/role-file-shape.md`, the one the census found

**Find** (a clause; one occurrence):

```text
The authority object is the spec the invoking prompt carries,
```

**Replace with:**

```text
The authority object is the spec the invoking prompt names,
```

**One word, and it is the file that tells anyone authoring a role file what a job statement may
say.** Left alone, the next role file written inherits the retired model at its opening sentence.

## Item 20 — `writer.md` gains the prohibition and loses the stale model

**Supersedes amendment 6's item 17h, in part.** Two corrections in the file of the role that
actually opens board files.

**The asymmetry is the substantive half.** `coach.md` carries the rule in **Boundaries** as a
prohibition. `writer.md` carries it in **Owns** as a statement of what the prompt does, and its
Boundaries gained no board line. **A `writer` reading only its own file learns that paths arrive
named; it does not learn that deriving one is refused.** That is backwards, since `writer` is the
role with the most board reads.

### 20a — the identity opener, three lines above item 17h's bullet

**Find** (a clause; one occurrence):

```text
You execute the process spec the
invoking prompt carries —
```

**Replace with:**

```text
You execute the process spec the
invoking prompt names —
```

### 20b — the Boundaries prohibition

**Find** (one list item, unique in the file):

```text
- Never edit a signed spec or an amendment, even though the board docs sit in your write
  surface. Both are `coach`'s artifacts, not yours.
```

**Replace with:**

```text
- Never edit a signed spec or an amendment, even though the board docs sit in your write
  surface. Both are `coach`'s artifacts, not yours.
- Never derive a board path. Read the ones your prompt names, and nothing else under
  `backlog/`.
```

**Two sentences, under `Instruction.ListItemSentences`'s three.** Measured, and the probe that
confirms the rule is live on this path drew a finding at four.

## Item 21 — `orchestration.md` cites rather than restates

**Supersedes amendment 6's item 17f in whole.** This is the only wholesale reversal in the chain,
and it is deliberate.

**The shape is the one `prose.md` names.** The carve-out sentence sits immediately before
"CLAUDE.md's Idea board section carries the procedures" — a summary beside its own pointer, in an
article whose charter says nothing there restates a role's own work. `editor` called it arguable,
since a role boundary is plausibly seat conduct. **I rule it a restatement**, on the ground that
the sentence beside it already routes the reader and the rule is a role boundary rather than seat
conduct.

**It is also the home cut the convergence ruling rests on.** Five homes become four.

### 21a — the restatement becomes a pointer

**Find** (two sentences; one occurrence):

```text
No role reads the board, beyond its own item's `spec.md` and `amendment-*.md` at paths the prompt names. A role never derives a board path, and every other item reaches a role as prompt content.
```

**Replace with:**

```text
`.claude/references/pipelines.md` states the one read carve-out and its limits.
```

**Nothing the seat needs is dropped.** The seat reads CLAUDE.md unconditionally, which carries
the carve-out at item 17d's anchor, and this sentence names the canonical file.

### 21b — the vocabulary collision, fixed at its source

`orchestration.md` says "The judging pass rules one of the four"; `/idea-assess` says "The judging
pass never rules and never promotes", which predates this slice. Both are right in their own
sense — ruling a disposition against recording the human ruling — and the readiness reference uses
the first sense too. **Read cold the two are flatly opposed.**

**Ruled: fix the skill, not the article.** Three files use "rules" for the disposition and one
uses it for the human ruling. The minority spelling moves.

**Find** (a whole line, unique in the file):

```text
- The judging pass never rules and never promotes. Both are separate human-invoked commands.
```

**Replace with:**

```text
- The judging pass rules the disposition and nothing else. The human ruling and the promotion are separate human-invoked commands.
```

**Both prohibitions survive.** "Never rules" becomes "rules the disposition **and nothing
else**", and "never promotes" becomes the second sentence naming both separate acts.

## Item 22 — `/idea-promote`'s step 6 stops erroring

**Supersedes amendment 6's item 14, in part. This is my own item, and the arithmetic is mine.**

Step 4 reads "absent one already" — conditional. The paragraph below asserts "Step 4 is
unconditional". **They disagree, and step 6 commits steps 4 and 5 together on the assumption one
of them did something.** Where the proposal already carries `kind:` and needs no fleshing out,
step 6 commits nothing and the command errors.

**Find** (a numbered step and a clause, both unique in the file):

```text
6. Commit steps 4 and 5 together, separately from the move commit.
```

**Replace with:**

```text
6. Commit whatever steps 4 and 5 changed, separately from the move commit. Skip it if neither changed anything.
```

**Then find:**

```text
**`kind:` is written after the move, never before it.** Step 4 is unconditional, and runs whether or not step 5 has anything to do.
```

**Replace with:**

```text
**`kind:` is written after the move, never before it.** Step 4 does not wait on step 5, and runs whether or not the proposal needs fleshing out.
```

**The obligation item 14 landed survives and stops overstating.** What step 4 must not do is wait
on step 5; what it is not is unconditional, since a proposal that already carries `kind:` needs no
line added. Step 6 now tolerates the empty case rather than assuming it away.

## Item 23 — the probe rule takes the shape `prose.md` prescribes

**Supersedes amendment 6's item 16, in part.**

### The packed-bullet observation, ruled rather than dismissed

**The bullet is four sentences packing three instructions under one lead, in the file that says
prefer nested bullets to a packed sentence.** `Instruction.ListItemSentences` does not reach it,
because that style is off for `articles/**`. **So the zero I reported on `prose.md` is a scope
fact, not a clean reading**, and I said as much in amendment 6's own table.

**Ruled: restructure it, and the decision is forced rather than close.** `editor`'s two gaps have
to land in the instruction. Adding them to the packed form makes it six sentences under one lead.
**The additions force the nesting whatever one thinks of the original.**

### The two gaps, and why both belong in the instruction rather than the sidecar

**The pre-edit baseline.** A candidate probe reporting five on `prose.md` means nothing without
knowing the file read five before. `prose.md` carries that 150 lines below under a damage heading.
**A reader following the standing instruction alone reports a pre-existing finding as
self-inflicted**, which is the exact defect that point names.

**Where the copy may sit.** Told to apply a candidate at a path on the same glob for a role-file
rule, the obvious move is a copy under `.claude/agents/`, which `agent-doc-check` hands to the
frontmatter validator and reds. **That is a failure mode, and exempt class 1 keeps a failure mode
in the instruction file.** Routing it to the sidecar as evidence leaves a reader who never opens
the sidecar to walk into it.

### 23a — `.claude/agents/articles/prose.md`

**Find** (one list item, unique in the file):

```text
- **Probe a predicted Vale reading; never count by hand.** Apply the candidate at a path on the
  same `.vale.ini` glob and run `vale` over it. Confirm the probe discriminates by re-running it
  with the defect deliberately present. Probe the whole enabled rule set for that path, never
  only the rules you are watching.
```

**Replace with:**

```text
- **Probe a predicted Vale reading; never count by hand.**
  - **Take the pre-edit baseline at the same path first.** A candidate reporting five findings
    means nothing until you know the file reported five before.
  - **Apply the candidate at a path on the same `.vale.ini` glob** and run `vale` over it.
  - **Probe the whole enabled rule set for that path**, never only the rules you are watching.
  - **Confirm the probe discriminates** by re-running with the defect deliberately present.
  - **Put the copy where no gate reaches it.** A role-file copy under `.claude/agents/` reds
    `npm run agent-doc-check`'s frontmatter validator; a subdirectory there does not, since its
    roster scan never descends.
```

**Every clause of the replaced bullet survives**, and `editor` confirmed as the practitioner who
ran the probes that the instruction as written would have produced them, with "probe the whole
enabled rule set" as the load-bearing clause. That clause is now a bullet of its own rather than a
tail.

**This is a structural edit `format:check` vouches for either way.** Count before and after: five
top-level bullets in that list before, five after, plus five nested.

### 23b — `.claude/agents/articles/prose.meta.md`, the stranded referent

Mechanism 1 ends "amendment 4 moved the rule to Boundaries to clear it", and the paragraph never
says what item 3 added, so "the rule" has no antecedent. **Sidecars are Vale-exempt, not
reader-exempt.**

**Find:**

```text
three sentences for `coach.md`'s spec-artifact bullet and did not count the bolded lead. The
   file read 0 before that pass and 1 after, and amendment 4 moved the rule to Boundaries to clear
   it.
```

**Replace with:**

```text
three sentences for `coach.md`'s spec-artifact bullet and did not count the bolded lead. The
   fourth sentence item 3 added was the rule that an edit is placed by an anchor rather than by a
   line number. The file read 0 before that pass and 1 after, and amendment 4 moved that rule to
   Boundaries to clear it.
```

## Which pass the items run in

**One pass, nine files, one commit**, with `editor` CLEAN after it. No item depends on another.
`pipelines.md` takes items 18 and 19b at four anchors that do not overlap.

## Measured readings — baseline first, then candidate, then discriminator

**Measured 2026-09-20 in this worktree at tip `0e372c9`**, in place, restored from git afterwards.
`git status --porcelain` reported empty after the restore.

**The pre-edit baseline was taken at every path before any candidate was applied**, which is the
clause item 23a adds to the standing instruction. Two of the nine files are non-zero at baseline,
and without that reading their post-edit numbers would read as regressions.

| Path                                       | Baseline                     | With items 18 to 23              |
| ------------------------------------------ | ---------------------------- | -------------------------------- |
| `CLAUDE.md`                                | **19 warnings**              | **19**                           |
| `.claude/references/pipelines.md`          | 0 in 1 file                  | **0**                            |
| `.claude/references/role-file-shape.md`    | 0 in 1 file                  | **0**                            |
| `.claude/agents/writer.md`                 | 0 in 1 file                  | **0**                            |
| `.claude/agents/articles/orchestration.md` | 0 in 1 file                  | **0**                            |
| `.claude/agents/articles/prose.md`         | **5 warnings**               | **5, the same five**             |
| `.claude/agents/articles/prose.meta.md`    | 0, by exemption              | **0, by exemption**              |
| `.claude/skills/idea-assess/SKILL.md`      | 0 in 1 file                  | **0**                            |
| `.claude/skills/idea-promote/SKILL.md`     | 0 in 1 file                  | **0**                            |
| `npm run reference-check`                  | 538 files, 3,224 refs, clean | **538 files, 3,226 refs, clean** |
| `npm run agent-doc-check`                  | 55 / 8 / 32, clean           | **unchanged, clean**             |
| `npx prettier --check` on the nine files   | clean                        | **clean**                        |

**The reference count moves by two**, from item 18a's `backlog/ready/<slice>/spec.md` and item
21a's `.claude/references/pipelines.md`. Both resolve.

**`idea-approve`'s sanctioned `HistoricalNarration` is untouched** — no item reaches that file,
so its one finding stands as ruled at amendment 2's item 6.

**Four discriminators, each reporting**, and each run against the whole enabled rule set for its
path rather than the rule under suspicion:

| Probe                                                      | Reported                                                          |
| ---------------------------------------------------------- | ----------------------------------------------------------------- |
| Item 20b's new Boundaries bullet written in four sentences | `Instruction.ListItemSentences`, 4 sentences                      |
| Item 22's step 6 written past the 20-word cap              | `Procedure.ProcedureLength` 28 words, and `STE.SentenceLength` 27 |
| Item 18a's carve-out bullet written past the 25-word cap   | `STE.SentenceLength`, 42 words                                    |
| Item 23a's bullet written past the 25-word cap             | `STE.SentenceLength`, 27 words                                    |

**Two earlier probe attempts did not discriminate, and that is recorded rather than quietly
replaced.** My first variants for `pipelines.md` and `prose.md` measured 24 and 20 words, both
under the cap, so they reported nothing. A probe that fails to discriminate is not evidence the
rule is absent; it is evidence the probe was too weak. Both were rewritten past the cap and
re-run.

## Sign-off

The user signs this amendment before `writer` runs. **The file set widens by one**, to
`.claude/references/role-file-shape.md`, forced by item 19's census. **Nothing here reaches
`scripts/`, `src/`, `vale-styles/` or `.vale.ini`.**

**Two things to weigh at signature beyond the items.** Item 21 undoes one item of amendment 6 in
whole. And the convergence ruling commits this slice to a stopping condition — after this
amendment, a non-blocking finding goes to the retrospective rather than to an eighth amendment.
**One sentence overturns either.**
