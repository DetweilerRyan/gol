---
name: a-survivor-no-input-can-distinguish-has-no-ruling
assessed: 2026-09-22
idea-blob: 1cd83ebbc2a08d8125c22ebcc24602602923f3dd
---

# Assessment — a-survivor-no-input-can-distinguish-has-no-ruling

`/idea-assess`, 2026-09-22. Kind: checker-bearing, enabler-process (SAFe: architecture or infrastructure
enabler). Disposition: Ready.

`LAYER1 backlog/ideas/a-survivor-no-input-can-distinguish-has-no-ruling.md: 7 checks, 0 findings`

Layer 1 also measured, on 2026-09-22: lane `ideas`, SCQA shape present, 66 lines, 0 `depends-on` mentions.

The slice check passed. The title rules nothing out, and the file says of itself that it is work rather than an
index — it is child 2 of the epic ruled on 2026-09-22 in
`mutation-runs-score-programs-typescript-forbids.assessment.md`.

The kind check asked whether a `product` VERIFY pass would have anything to observe. It would not. Every
finished state the file names is text in an instruction file or a comment at a mutation survivor's site. The
sub-kind resolves cleanly: the diff lands in `.claude/agents/articles/mutation-testing.md`, or its sidecar, which
is the instruction tier rather than the tree the gates measure. So **enabler-process**, and V and T are scored
against the checker-bearing column.

| Letter      | Score | Finding                                                                                      |
| ----------- | ----- | -------------------------------------------------------------------------------------------- |
| Independent | 3     | Argues the sibling does not block on substance, then declares a co-landing nobody has ruled. |
| Negotiable  | 5     | Shapes rather than specifies, and states the condition that would make the idea moot.        |
| Valuable    | 5     | Names the false record that survives without it, with a dated instance and a durable class.  |
| Estimable   | 4     | Bounds the doc surfaces and names five unknowns; leaves the survivor-site reach unstated.    |
| Small       | 4     | Trips no design-pass trigger; the reach is one article section and possibly its sidecar.     |
| Testable    | 2     | Names a check that belongs to the argument form, not one whose reading the slice moves.      |

## Independent

The file names one sibling — the technical enabler that wires the Stryker TypeScript checker into both configs —
and argues on the record that it does not block: adopting the checker removes the 11 type-impossible survivors
standing on 2026-09-22 but not the class, and the TS6133 case on `liveCellStore.ts` is cited as a survivor the
repo's own build rejects while the checker accepts. On substance that argument holds, and the file says this is
"the only one of the two that delivers on its own".

Two things keep the letter at 3 rather than 4.

The blocking status is asserted rather than checked. The No-gos also declare that "Both land on one branch and
merge together". Read against CLAUDE.md as of 2026-09-22 — one slice, one worktree, one branch — that is a
stated coupling to an unlanded item, and the two halves run different pipelines, since this one is
enabler-process and the sibling is enabler-technical. Nothing in the tree has ruled whether one branch may carry
two pipelines' cycles. The file therefore says both that it waits on nothing and that it ships alongside a
sibling, and does not say which governs a promotion.

The sibling is also named by description rather than by slug. Two candidates sat in `backlog/ideas/` on
2026-09-22 whose titles both open "Make the mutation gate type-aware" —
`mutation-runs-score-programs-typescript-forbids.md`, the parent ruled Epic, and
`the-mutation-gate-scores-programs-that-cannot-exist.md`, the technical child. Only git's commit
`9170d29 backlog: split the type-aware-mutation epic into its two children` disambiguates which one "the sibling
technical enabler" means.

Not blocking. Both halves close with a ruling by the seat at worktree setup, not with knowledge nobody has, and
the spike test for I — check whether the named dependency actually blocks — has nothing to check.

## Negotiable

The Answer is labelled "Shaped, not specified" and offers a closing form sitting beside the equivalence rule
rather than replacing it. It records that `architect` recommended on 2026-09-22 that such an argument be made
against the repo's own compiler configuration, and then states plainly that nothing has ruled it. Five open
questions carry the rest, including who holds the ruling and where the form lives.

The letter reaches 5 on the Complication's last paragraph, which states the condition that would rule the idea
out on its own terms: if adopting the checker removed the class and not merely the 11 instances, there would be
nothing left to write a form for. The file names that falsifier and answers it with the TS6133 measurement
rather than leaving it implicit. The falsifier arrives as a rebuttal rather than as a standing test, which is a
presentation difference and not a scoring one.

## Valuable

Scored against the checker-bearing column.

The false claim that survives without this slice is named exactly: a role meeting a type-impossible survivor
"either gives up without a record, or records an equivalence argument that is not the argument it actually has".
The second branch is the expensive one, because the repo's convention as of 2026-09-22 is that an equivalence
argument sits as a comment at the survivor's own site, where a later reader takes it as checked reasoning.

The live instance is cited and dated. `slice/type-impossible-mutants-read-as-survivors` measured on 2026-09-22
that 11 of 61 survivors across both Stryker configs are forbidden by the type system — 6 of 23 on `src/` and 5
of 38 on `scripts/` — and its record sits in `backlog/done/type-impossible-mutants-read-as-survivors/`. The file
does not restate that spike's method, and points at it instead.

The letter reaches 5 because the instance count is dated and measured rather than asserted, and because the file
separates the transient instance from the durable class. The 11 are removed by the sibling; the TS6133 case on
`liveCellStore.ts` is not, since it fails the repo's own build and compiles under the three compiler options the
checker relaxes. That is the measurement that makes this slice's value outlive the sibling's.

## Estimable

The surfaces are named. The form sits beside the equivalence rule in `.claude/agents/articles/mutation-testing.md`,
or in `mutation-testing.meta.md`, and the first open question states that both candidates are live and that the
parent spike left the choice open deliberately. The No-gos fence off the two Stryker configs and the equivalence
rule itself. So the bound is one article section, possibly one sidecar section, and a site-comment convention.

Four of the five open questions are unknowns that could move that bound: where the form lives, which compiler
configuration the argument is made against, who holds the ruling, and whether a `CompileError` whose only type
error sits in a test file needs its own entry. None of them multiplies the file count.

Not 5, because the unknown that would move the bound most is the one the file does not name. It does not say
whether the slice also applies the new form to the 11 survivors standing on 2026-09-22. If it does, the reach
crosses out of `.claude/**` into comment surfaces in both `src/` and `scripts/` at up to 11 sites, and the
sub-kind stops being purely enabler-process. Open question 4 asks what the site comment says without saying
whether this slice writes any.

## Small

Scored against the design-pass checklist CLAUDE.md states. The reach trips none of its five triggers: no
behaviour-preserving refactor, no module created, moved or split, no crossing of the framework-free module →
hook → component layering, no file flagged oversized, and no span across three or more modules. The whole
deliverable is prose in the instruction tier. So 4.

Not 5, and for the same reason Estimable stopped at 4. While it is unstated whether the slice applies the form
to the 11 survivors measured 2026-09-22, a split between authoring the form and applying it has something to
separate. Ruling that one question closes the gap in both letters at once.

The Epic test does not fire. Splitting this file would produce children that inherit its own weak Testable
rather than improving on it, which the article names as the signal that a file is under-examined rather than
oversized. The parent epic already performed the split that mattered.

## Testable

Scored against the checker-bearing column, and this is the file's weakest letter. The parent epic's assessment
of 2026-09-22 predicted it, recording that this child's "own Testable is the thing it must solve rather than
inherit".

The file names no command whose reading changes when the slice lands, no reading on 2026-09-22, and no reading
afterwards. Anchor 1 is defensible on a strict reading. The score is 2 on the generous one, because a check is
named — the repo's own compiler configuration reporting TS6133 on the mutated program, measured against
`liveCellStore.ts` on 2026-09-22. That check belongs to the _argument form the slice must define_ rather than to
the slice's own finished state, and open question 2 leaves even its status unruled.

What raises the letter exists and is written down, which is why this is a revision rather than a spike. The
enabler-process pipeline's acceptance is stated in `.claude/references/pipelines.md`, read 2026-09-22: `coach`
REVIEW reads the landed corpus against the signed spec, and `editor` AUDIT runs `npm run prose-lint`,
`npm run agent-doc-check` and `npm run reference-check`. The deliverable also has an observable of its own that
the file already half-names — whether the 11 survivors measured 2026-09-22 can each be closed with a recorded
argument, which open question 4 approaches from the comment's side without stating it as a reading.

The board carries the precedent. `backlog/ideas/stand-up-the-rca-retrospective.assessment.md` scored this same
letter 2 against blob `e72cfee` on the identical finding, that the file named no check whose reading changes,
and a revision raised it to 5 with four checks and their dated readings. No spike ran.

Not blocking, on the judgement that the gap closes at the pipeline's own first step rather than before it.
`coach` SPEC is the enabler-process pipeline's opening pass, it translates a shaped proposal into a file-by-file
edit spec, and `.claude/references/pipelines.md` places the user's sign-off gate at that step's close. A file
labelled "Shaped, not specified" is the input state that step is built for, and the five open questions are its
agenda. A reader who holds that a checker-bearing candidate must carry its own reading before promotion would
rule differently on this letter, and the human ruling below is where that disagreement belongs.

One finding against the anchors rather than against the file, recorded for the calibration sidecar rather than
acted on here. `definition-of-ready.md`'s spike test names "revise" as an outcome, and the four-disposition
table has no box for it. Every finding above is a writing or ruling task rather than a knowledge gap, so the
table forced a choice between Ready and a spike the test forbids.

## What happens next

Promote.

## The human ruling

Ruled 2026-09-22.

Independent — agree.
Negotiable — agree.
Valuable — agree.
Estimable — agree.
Small — agree.
Testable — agree.
