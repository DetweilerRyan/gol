---
name: split-enablers-and-staff-the-process-pipeline
title: 'Index: the process-pipeline shaping, split into a technical and a process child'
created: 2026-09-17
---

**This file is an index, not work** — the slice check's second exemption in
`.claude/references/definition-of-ready.md`. It carried the user's 2026-09-17 shaping
conversation as one candidate and was ruled an **Epic** by that day's `/idea-assess` run,
whose sharpest finding was self-referential: the file's own sub-kind discriminator sorted its
own deliverables into both kinds (the checker change is a script edit, not `writer`-legal).
The full ruling set lives in this file's git history at the split commit's parent. It is
itself child 5 of `act-on-the-pipelines-feedback`.

## The children

In the suggested landing order:

| #   | Child                                                                               | Carries                                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `backlog/ready/declare-the-role-cycles-in-config/proposal.md` (promoted 2026-09-17) | The `enabler-technical` half: the cycle-declarations config, the check-4 checker change, and the allowlist entry argued by the user's config-invariance principle. First, so child 2's role docs never need the decorated-chain interim. |
| 2   | `backlog/ideas/staff-the-enabler-process-pipeline.md`                               | The process-staffing proper: the sub-kind split with the DoR kind-check edit, the `coach`/`writer`/`editor` role files, the user-gated pipeline, the amended no-role-edits rule, and the retro reservation.                              |

## Open questions

- The sibling audit `role-files-assume-the-story-pipeline` (child 3 of the feedback epic)
  gains a third pipeline to phrase against once child 2 lands — its sequencing relative to
  child 2 is child 2's open question.
- The config-invariance principle the user ruled travels with child 1; whether it later
  generalizes to other checker configs is nobody's current work.
