---
name: mutation-runs-score-programs-typescript-forbids
assessed: 2026-09-22
idea-blob: 0ecef2c8e3f06e1fd7014dccdde6125e2b8929d2
---

# Assessment — mutation-runs-score-programs-typescript-forbids

`/idea-assess`, 2026-09-22. Kind: epic — the kind check was not reached, so no orientation from its three
columns applies; `epic` is CLAUDE.md's own frontmatter label, and SAFe's term for the same thing is epic.
Disposition: Epic.

`LAYER1 backlog/ideas/mutation-runs-score-programs-typescript-forbids.md: 7 checks, 1 findings`

Layer 1 also measured, on 2026-09-22: lane `ideas`, SCQA shape present, 108 lines, 0 `depends-on` mentions.
Its one finding was a stale record: the assessment standing before this one carried `idea-blob` `e9ff668`,
against the file's blob `0ecef2c` as read on 2026-09-22. That earlier record ruled Epic and named three
children; the file has since grown a Children section naming two, and recording the user's 2026-09-22 ruling
on the branch. This pass replaces the record whole. The earlier record carried `None recorded.` under its
human-ruling heading, so no ruling was discarded.

## The slice check ruled this file exempt, and no letter is scored

The file says of itself, under its Children heading, that it was ruled an Epic on 2026-09-22, that it stays an
index in `ideas/`, and that it takes no `ready/` folder. That is the slice check's index class verbatim: the
file says of itself that it is not work. An exempt file never reaches the kind check, and scoring one is a
finding against the assessor rather than against the file. So this pass scores no letter and rules the
disposition the slice check routes an index to.

Nothing here re-opens the earlier record's six numbers. They were taken on 2026-09-22 against blob `e9ff668`,
a file that had not yet declared itself an index, and they are history rather than a reading of the tree
today. The scoring that matters now sits in the two children's own records, each taken against its own file.

| Letter      | Score      | Finding                                                                                |
| ----------- | ---------- | -------------------------------------------------------------------------------------- |
| Independent | not scored | Exempt at the slice check; the children carry their own dependency readings.           |
| Negotiable  | not scored | Exempt at the slice check; an index proposes a split rather than a solution.           |
| Valuable    | not scored | Exempt at the slice check; the evidence travelled to the adoption child.               |
| Estimable   | not scored | Exempt at the slice check; an index bounds its children's sizes, not its own.          |
| Small       | not scored | Exempt at the slice check; the split this letter would have forced is already written. |
| Testable    | not scored | Exempt at the slice check; each child names the check its own thread moves.            |

## Independent

Declined. The file is an index, exempt from assessment at the slice check, so it takes no score on this
letter. The dependency question it would have asked is answered in the children's records instead — both were
assessed on 2026-09-22, and each reads the parent spike's landed state for itself.

## Negotiable

Declined, for the same exemption. An index's job is to hold a split open, not to hold a solution open. The
file's Question hands the split ruling outward and its Answer is marked shaped rather than specified, but
neither is a reading this pass is entitled to score.

## Valuable

Declined, for the same exemption. The measured evidence the earlier record scored here — the 11
type-impossible survivors of 61 across both configs, dated 2026-09-22 — is restated in the file's Situation
and travelled to the adoption child, where it is scored against that child's own reach.

## Estimable

Declined, for the same exemption. An index bounds nothing of its own; each child's record bounds its own
surface. Note that E never spikes, so nothing about this declination routes anywhere but the split.

## Small

Declined, for the same exemption, and this is the letter the exemption stands in for. The Epic test the
earlier record ran on 2026-09-22 concluded the children would each score better apart, and the split has since
been written into the file. Re-scoring S would be re-running a ruling the file already carries.

## Testable

Declined, for the same exemption. The three threads the file names sit at different distances from a check,
which is why the earlier record read T as an aggregate. Each child now names the check its own thread moves,
and those readings are the live ones.

## What happens next

The file stays in the `ideas/` lane as an index and gets no `ready/` folder. Its Children table names two
children, both already captured on the board and both assessed on 2026-09-22 with no human ruling yet:

1. `the-mutation-gate-scores-programs-that-cannot-exist` — kind `enabler-technical`. Carries both Stryker
   configs, the `@stryker-mutator/typescript-checker` package, and the dark-file guard as a separable
   sub-thread. Its own record rules Ready.
2. `a-survivor-no-input-can-distinguish-has-no-ruling` — kind `enabler-process`. Carries the closing form for
   a survivor no input can distinguish. Its own record rules Ready.

Two departures from the earlier record are the file's own, recorded in it rather than ruled here. The third
child that record named — a guard for a file whose every mutant is excluded — is not a separate child; the
file folds it into the adoption child as a sub-thread that child's assessment may still reject. And the user
ruled on 2026-09-22 that both children land on one branch, `make-the-mutation-gate-type-aware`, and merge
once, so the board holds two `ready/` folders pointing at one branch. The file states that this departs from
one slice to one item and that nothing enforces it.

Nothing further is owed by this file. The next acts belong to the children: a human ruling on each record,
then promotion.

## The human ruling

None recorded.
