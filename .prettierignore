node_modules
dist
dist-ssr
dist-perf
coverage
coverage-scripts
reports
.stryker-tmp
.stryker-tmp-scripts
.vitest-attachments
package-lock.json

# playwright-bdd generated specs
.features-gen

# Per-slice git worktrees: another slice's checkout, not this one's source
.claude/worktrees

# ast-grep regenerates these; formatting them would churn on every `npm run ast-grep:test`
rule-tests/__snapshots__/

# Vale's synced style package: third-party YAML this repo does not author.
# Gitignoring is not sufficient -- Prettier does not consult .gitignore.
.vale
