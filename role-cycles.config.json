{
  "$schema": "./schemas/role-cycles.schema.json",
  "cycles": [
    {
      "pipeline": "story",
      "roles": ["product", "coder", "cleaner", "architect", "hardener", "product"]
    },
    {
      "pipeline": "process",
      "roles": ["coach", "writer", "editor", "coach"]
    }
  ]
}
